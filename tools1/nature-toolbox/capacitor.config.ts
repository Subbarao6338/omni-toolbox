import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nature.toolbox.ultimate',
  appName: 'Nature Toolbox',
  webDir: 'dist'
};

export default config;
