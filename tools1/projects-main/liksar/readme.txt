Inside this elixir directory. you can see two different directory with name "exlixir" and "regex_generator".
elixir containts the main elixir development and functionalities .
In regex_generator you can find the regex generator code (in java language).