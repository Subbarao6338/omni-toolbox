import React from "react";
import { Button } from "react-bootstrap";

function Intro() {
  return (
    <Button className="btn btn-secondary" style={{ width: "100px" }}>
      Show
    </Button>
  );
}

export default Intro;
